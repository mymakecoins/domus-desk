<?php
/**
 * English (en) — Workflows module strings.
 *
 * Workflows is a cross-module automation engine: triggers fire when events
 * happen in other modules (a ticket is created, a form is submitted),
 * conditions filter which workflows respond, and actions do something
 * (update a record, send an email, call a Graph endpoint).
 */
return [
    'title' => 'Workflows',

    'nav' => [
        'workflows'  => 'Workflows',
        'executions' => 'Execution log',
        'webhooks'   => 'Webhook deliveries',
        'settings'   => 'Settings',
        'help'       => 'Help',
    ],

    'executions' => [
        'page_title'    => 'Execution log',
        'intro'         => 'Every run of every workflow. The engine deliberately swallows its own errors so a broken rule can never break the ticket save that triggered it — which means a failing workflow is otherwise silent. This is where you find it.',
        'all'           => 'All',
        'f_workflow'    => 'Workflow',
        'f_status'      => 'Status',
        'f_trigger'     => 'Trigger',
        'f_dry'         => 'Type',
        'real_only'     => 'Real runs only',
        'dry_only'      => 'Dry runs only',
        'f_from'        => 'From',
        'f_to'          => 'To',
        'f_search'      => 'Search',
        'f_search_ph'   => 'Workflow name, trigger or error…',
        'clear'         => 'Clear',
        'col_status'    => 'Status',
        'col_workflow'  => 'Workflow',
        'col_trigger'   => 'Trigger',
        'col_when'      => 'When',
        'col_took'      => 'Took',
        'col_detail'    => '',
        'view'          => 'View',
        'none'          => 'No runs match those filters.',
        'showing'       => '%t run(s)',
        'range'         => '%a–%b of %t',
        'run'           => 'Run',
        'error'         => 'Error',
        'steps'         => 'What happened',
        'no_steps'      => 'No steps recorded.',
        'deliveries'    => 'Webhooks this run queued',
        'open_webhooks' => 'Open the delivery log',
        'payload'       => 'Trigger payload',
        'payload_hint'  => 'Exactly what the conditions were tested against, and what the {{variables}} resolved from — captured at run time.',
        'passed'        => 'matched',
        'did_not_match' => 'did not match',
        'actual'        => 'actual value:',
    ],

    'settings_tabs' => [
        'ai'      => 'AI',
        'formats' => 'Message formats',
    ],

    'formats' => [
        'title'         => 'Webhook message formats',
        'intro'         => 'A chat format is nothing more than a <strong>JSON body template with a <code>{{message}}</code> slot</strong> — Slack is <code>{"text": "{{message}}"}</code>, Discord is <code>{"content": "{{message}}"}</code>. So they live here as data rather than in code, and you can add any platform that accepts an incoming webhook — Google Chat, Mattermost, Rocket.Chat, Zulip, ntfy — without waiting for a release.',
        'reserved_note' => '<strong>Custom (raw JSON)</strong> and <strong>Full record</strong> aren\'t listed here. They don\'t wrap a message &mdash; one sends the exact JSON you write, the other sends the whole record &mdash; so they stay part of the engine rather than pretending to be formats.',
        'col_name'      => 'Name',
        'col_key'       => 'Key',
        'col_template'  => 'Body template',
        'col_status'    => 'Status',
        'builtin'       => 'Built-in',
        'copy'          => 'Copy',
        'add_title'     => 'New message format',
        'edit_title'    => 'Edit message format',
        'f_label'       => 'Name',
        'f_key'         => 'Key',
        'f_key_hint'    => 'Lowercase letters, numbers, hyphens and underscores. Stored inside every workflow that uses this format, so avoid changing it later.',
        'f_template'    => 'JSON body template',
        'f_template_hint' => 'The exact JSON this platform expects, with <code>{{message}}</code> where your workflow\'s message should go. You can use any merge code here too (e.g. <code>{{ticket.number}}</code>). Quotes and newlines in the message are escaped for you &mdash; you never need to worry about breaking the JSON.',
        'f_url_pattern' => 'URL pattern (optional)',
        'f_url_pattern_hint' => 'A regular expression matching this platform\'s webhook URLs, e.g. chat\\.googleapis\\.com. If the URL on a workflow doesn\'t match, the editor warns you — which catches the classic "pasted a Discord URL but left the format on Slack".',
        'f_hint'        => 'Formatting hint (optional)',
        'f_hint_hint'   => 'One line about this platform\'s markdown, shown under the Message box. Worth filling in: Discord treats *one asterisk* as italic where Slack treats it as bold, and that difference costs people real time.',
        'saved'         => 'Message format saved.',
        'deleted'       => 'Message format deleted.',
        'delete_confirm'=> 'Delete this message format?',
        'mismatch'      => 'This URL doesn\'t look like a %f webhook URL. Check you picked the right format — the endpoint will reject a payload in the wrong shape.',
        'mismatch_known'=> 'This looks like a %u webhook URL, but the format is set to %f. The endpoint will reject it — switch the format to %u.',
    ],

    'ai_settings' => [
        'title'                   => 'AI integration',
        'intro'                   => 'Configure the AI provider that powers the workflow co-author. Each module has its own key so billing and access can be granular. The API key is encrypted at rest. Use Test to verify your key + model before saving.',
        'provider_label'          => 'Provider',
        'model_label'             => 'Model',
        'model_hint'              => 'Pick a suggestion or paste any model id supported by the chosen provider.',
        'key_label'               => 'API key',
        'key_placeholder_empty'   => '(no key stored — paste a fresh one to set)',
        'key_placeholder_stored'  => 'Stored — leave unchanged to keep',
        'key_hint'                => 'Encrypted at rest. Leave blank or unchanged to keep the existing key.',
        'verify_ssl_label'        => 'Verify SSL',
        'verify_ssl_hint'         => 'Disable only for testing against environments with self-signed certificates (e.g. behind an inspecting proxy).',
        'ssl_warning_title'       => 'Warning',
        'ssl_warning_body'        => 'SSL verification is turned off. Domus Desk will accept any TLS certificate from the AI provider without checking it. Anyone with access to your network could pose as the provider, intercept the traffic, and steal your API key — along with every prompt and response that follows. Only leave this off in test environments with self-signed certificates — never in production.',
        'test_btn'                => 'Test',
        'testing'                 => 'Testing…',
    ],

    'list' => [
        'page_title'      => 'Workflows',
        'intro'           => 'Automation rules that listen for events from other modules and act on them. Each rule is a trigger, optional conditions, and one or more actions.',
        'add_btn'         => 'New',
        'no_workflows'    => 'No workflows yet — click "New" to create your first automation.',
        'col_name'        => 'Name',
        'col_trigger'     => 'Trigger',
        'col_actions'     => 'Actions',
        'col_last_run'    => 'Last run',
        'col_status'      => 'Status',
        'col_row_actions' => 'Actions',
        'never_run'       => 'Never',
        'active'          => 'Active',
        'inactive'        => 'Inactive',
    ],

    'editor' => [
        'new_title'       => 'New workflow',
        'edit_title'      => 'Edit workflow',
        'name_label'      => 'Name',
        'name_placeholder'=> 'e.g. P1 ticket alert',
        'description_label' => 'Description',
        'description_placeholder' => 'Optional — explain what this workflow does and why',
        'trigger_label'   => 'Trigger',
        'trigger_hint'    => 'The event that fires this workflow.',
        'conditions_label'=> 'Conditions',
        'conditions_hint' => 'Optional. All conditions must match for the actions to run. Leave empty to run on every trigger event.',
        'actions_label'   => 'Actions',
        'actions_hint'    => 'Run these in order, top to bottom. At least one action is required.',
        'active_label'    => 'Active — run when the trigger fires',
        'add_condition'   => 'Add condition',
        'add_action'      => 'Add action',
        'remove'          => 'Remove',
        'test_fire'       => 'Test fire',
        'test_fire_hint'  => 'Run this workflow once with a synthetic payload so you can verify the actions work. Real triggers will fire when wired in subsequent commits.',
        'dry_run'         => 'Dry run',
        'dry_run_hint'    => 'Check the conditions and show what each action would do — without doing it. Nothing is written, emailed or sent.',
        'no_conditions'   => 'No conditions — runs on every event.',
        'no_actions'      => 'No actions yet — add at least one.',
        'condition_field' => 'Field',
        'condition_op'    => 'Operator',
        'condition_value' => 'Value',
        'action_type'     => 'Action',
        'action_args'     => 'Arguments (JSON)',
        'back'            => 'Back',
    ],

    'op' => [
        'equals'      => 'equals',
        'not_equals'  => 'does not equal',
        'in'          => 'is one of',
        'not_in'      => 'is not one of',
        'contains'    => 'contains',
        'not_contains'=> 'does not contain',
        'gt'          => 'greater than',
        'lt'          => 'less than',
        'is_empty'    => 'is empty',
        'is_not_empty'=> 'is not empty',
    ],

    'status' => [
        'success' => 'Success',
        'failed'  => 'Failed',
        'skipped' => 'Skipped (conditions did not match)',
        'running' => 'Running',
        'aborted' => 'Aborted (loop protection)',
    ],

    'diagnosis' => [
        'fix_link' => 'How to fix this',
    ],

    'vars' => [
        'open'              => 'Edit with variables',
        'title'             => 'Edit text',
        'picker_heading'    => 'Insert a variable',
        'picker_hint'       => 'Click to drop it in at the cursor. Only the variables this trigger can actually fill in are listed.',
        'search_placeholder'=> 'Search variables…',
        'no_match'          => 'No variable matches that search.',
        'preview'           => 'Preview',
        'preview_empty'     => 'Nothing to preview yet.',
        'unknown'           => '%s will come out blank — not available on "%t".',
    ],

    'dry_run' => [
        'pill'        => 'Dry run',
        'would_have'  => 'This run would have:',
        'no_actions'  => 'Nothing would have run — the conditions did not match.',
    ],

    'templates' => [
        'btn'           => 'New from template',
        'title'         => 'Start from a template',
        'intro'         => 'Ready-made workflows for the jobs most service desks automate first. Pick one and it is copied into your workflows — switched off, so you can read it, adjust it and dry-run it before it fires for real.',
        'when'          => 'When:',
        'then'          => 'Then:',
        'use'           => 'Use this template',
        'needs_config'  => 'This template needs a few details from you before it will work:',
        'filter_cat'    => 'Category',
        'f_search'      => 'Search',
        'all_cats'      => 'All categories',
        'search_ph'     => 'Search recipes…',
        'no_match'      => 'No recipe matches that. Try a different search, or clear the filters.',
        'showing'       => 'Showing {n} of {total}',
    ],

    'toast' => [
        'saved'         => 'Workflow saved.',
        'deleted'       => 'Workflow deleted.',
        'delete_confirm'=> 'Delete this workflow?',
        'fire_started'  => 'Test fire started — see the Execution log.',
        'fire_done'     => 'Test fire complete: %s.',
        'fire_failed'   => 'Test fire failed: %s',
        'dry_run_started' => 'Dry run started — nothing will actually be changed.',
        'dry_run_done'    => 'Dry run complete: %s. See Recent runs for what would have happened.',
        'run_unsaved'   => 'Note: you have unsaved changes — this runs the last saved version.',
        'name_required' => 'Name is required.',
        'actions_required' => 'At least one action is required.',
        'saved_no_actions' => 'Saved — but this workflow has no actions yet, so it won\'t do anything until you add one.',
        'ai_applied'      => 'AI proposal applied to the canvas. Tweak then save.',
        'ai_failed'       => 'AI co-author failed: %s',
        'saved_settings'  => 'Settings saved.',
    ],

    'ai' => [
        'btn'              => 'AI co-author',
        'modal_title'      => 'AI co-author',
        'intro'            => 'Describe the workflow you want and the AI will scaffold it on the canvas. You can keep going — say "now add a condition for…" and it iterates on what\'s there.',
        'prompt_label'     => 'What should this workflow do?',
        'prompt_placeholder' => 'e.g. When a P1 ticket from Finance is created, log a message saying "P1 finance ticket — alert team"',
        'generate'         => 'Generate',
        'thinking'         => 'Thinking…',
        'apply'            => 'Apply to canvas',
        'discard'          => 'Discard',
        'close'            => 'Close',
        'iterate_hint'     => 'Tip: when you have a workflow on the canvas, the AI iterates on it. Try "make it only match Finance, not all departments" or "add an action to log the ticket id too".',
        'explanation_label' => 'What I built',
        'preview_label'    => 'Proposed workflow',
        'warnings_label'   => 'Notes',
        'only_log_message' => 'Only the log_message action is implemented today; the AI will lean on it as a placeholder for unimplemented actions (e.g. "send email" → log_message documenting the intent).',
    ],

    'help' => [
        'page_title' => 'Workflows guide',
        'intro'      => 'Workflows let you automate the things you find yourself doing manually after a ticket arrives: tagging, escalating, assigning, notifying, fanning out to other systems. A workflow listens for an event, optionally filters with conditions, then runs one or more actions in order.',
        'guide'      => 'Guide',

        'nav_templates'  => 'Start from a template',
        'nav_ssl'        => 'HTTPS certificates',
        'nav_wiki'       => 'Workflows on the wiki',
        'nav_anatomy'    => 'Anatomy of a workflow',
        'nav_canvas'     => 'The visual canvas',
        'nav_conditions' => 'Conditions',
        'nav_actions'    => 'Actions',
        'nav_variables'  => 'Variables',
        'nav_ai'         => 'AI co-author',
        'nav_testing'    => 'Saving and testing',
        'nav_triggers'   => 'The trigger catalogue',
        'nav_webhooks_deepdive' => 'Webhooks guide',
        'nav_failures'   => 'Engine failures are isolated',
        'nav_ahead'      => 'What\'s still ahead',

        // 1. Anatomy
        'anatomy_heading' => '1. Anatomy of a workflow',
        'anatomy_intro'   => 'Every workflow has three parts:',
        'anatomy_trigger'    => '<strong>Trigger</strong> &mdash; the event that fires the workflow. Examples: <code>ticket.created</code>, <code>ticket.priority_changed</code>, <code>ticket.assigned</code>. Exactly one trigger per workflow.',
        'anatomy_conditions' => '<strong>Conditions</strong> <em>(optional)</em> &mdash; filters that decide whether the workflow runs. All conditions must match (AND semantics). <em>"Only when priority is Critical AND department is Finance."</em>',
        'anatomy_actions'    => '<strong>Actions</strong> <em>(one or more)</em> &mdash; the things to do when the trigger fires and the conditions pass. Run in order, top to bottom.',
        'anatomy_exec'    => 'Execution is synchronous &mdash; the workflow runs in the same web request that fired the event. Each run writes a row to the execution log with the full trigger payload, every condition\'s pass/fail, and every action\'s result.',

        // 2. Canvas
        'canvas_heading' => '2. The visual canvas',
        'canvas_intro'   => 'The editor is a dot-grid canvas with three node shapes:',
        'canvas_trigger'   => '<strong>Trigger node</strong> &mdash; amber pill, pinned at the top centre. One per workflow; can\'t be deleted.',
        'canvas_condition' => '<strong>Condition nodes</strong> &mdash; orange diamonds. Add one with <em>Add condition</em>. Drag to reorder.',
        'canvas_action'    => '<strong>Action nodes</strong> &mdash; blue rounded rectangles. Add one with <em>Add action</em>. Drag to reorder.',
        'canvas_order'   => 'Connectors are drawn automatically in execution order: trigger &rarr; conditions (top to bottom by y position) &rarr; actions (top to bottom). <strong>Position IS the order</strong> &mdash; drag a condition above another to swap them.',
        'canvas_panel'   => 'Click any node to open the detail panel on the right. Click empty canvas to switch the panel back to the workflow-level fields (name / description / active flag / recent runs). Right-click or press Delete to remove the selected node (except the trigger).',

        // 3. Conditions
        'conditions_heading' => '3. Conditions',
        'conditions_intro'   => 'The condition panel has three controls: <strong>Field</strong>, <strong>Operator</strong>, <strong>Value</strong>. Their behaviour adapts to the chosen field type:',
        'conditions_lookup_heading' => 'Lookup fields (priority, status, department, type, analyst&hellip;)',
        'conditions_lookup_body'    => 'The value control is a <strong>checkbox list of real values</strong> pulled from the lookup table &mdash; not opaque ids. Tick one for an exact match, tick several for OR semantics (e.g. <em>"priority is Critical OR High"</em>). The operator dropdown collapses to <strong>is / is not / is empty / is not empty</strong> &mdash; the multi-select handles the rest.',
        'conditions_text_heading' => 'Text fields (subject, requester email, title&hellip;)',
        'conditions_text_body'    => 'Plain text input with the full text-operator catalogue: <strong>equals / not equals / is one of / is not one of / contains / does not contain / is empty / is not empty</strong>. <code>gt</code> / <code>lt</code> are <em>not</em> offered &mdash; lexicographic string comparison is a footgun.',
        'conditions_num_heading' => 'Numeric fields (ids, durations&hellip;)',
        'conditions_num_body'    => 'Numeric operators: <strong>equals / not equals / is one of / is not one of / greater than / less than / is empty / is not empty</strong>. No <em>contains</em> &mdash; substring search on a number is meaningless.',

        // 4. Actions
        'actions_heading' => '4. Actions',
        'actions_intro'   => 'Nine handlers ship today. Pick an action\'s type from the dropdown and a labelled form appears below with the right widget per arg (text input, textarea, number input, lookup dropdown):',
        'actions_th_type'     => 'Type',
        'actions_th_does'     => 'What it does',
        'actions_th_args'     => 'Key args',
        'actions_row1_does' => 'Writes a message into this workflow\'s execution log. Useful as a placeholder while you scaffold a rule and as a way to verify the engine end-to-end.',
        'actions_row1_args' => 'message',
        'actions_row2_does' => 'Changes a ticket\'s status. Automatically stamps / clears <code>closed_datetime</code> when the new status\'s <em>is_closed</em> flag flips.',
        'actions_row2_args' => 'ticket_id, status_id',
        'actions_row3_does' => 'Sets a ticket\'s priority.',
        'actions_row3_args' => 'ticket_id, priority_id',
        'actions_row4_does' => 'Sets a ticket\'s owner / assignee. Updates both <code>assigned_analyst_id</code> and <code>owner_id</code> so the right-pane Owner field stays in sync.',
        'actions_row4_args' => 'ticket_id, analyst_id',
        'actions_row5_does' => 'Appends a free-text note to the ticket\'s audit trail. Internal &mdash; never sent to the requester.',
        'actions_row5_args' => 'ticket_id, note',
        'actions_row6_does' => 'Sends an email to the ticket\'s requester (or any address) using the ticket\'s mailbox. Subject is prefixed with <code>[SDREF:&hellip;]</code> so replies thread back automatically.',
        'actions_row6_args' => 'ticket_id, to, subject, body',
        'actions_row7_does' => 'Spawns a task. Auto-links to the source ticket if a <code>ticket_id</code> is supplied. Status / priority default to the workspace defaults if blank.',
        'actions_row7_args' => 'title, description, status_id, priority_id, assignee_id, ticket_id',
        'actions_row8_does' => 'Creates a new ticket. Useful for fan-out workflows like <em>"new-starter form &rarr; IT + HR + Facilities tickets"</em>.',
        'actions_row8_args' => 'subject, body, priority_id, department_id, type_id, assigned_analyst_id, from_email, from_name',
        'actions_row9_does' => 'POSTs a message to an external URL &mdash; push events into Slack, Teams, Discord, PagerDuty, Zapier/Make or any system that accepts an incoming webhook. Pick a chat preset, send the Full record (the whole object as JSON, same as the REST API), or template your own custom JSON body. Supports an optional HMAC signing secret, and a Send test button that shows the endpoint&rsquo;s real response before you save.',
        'actions_row9_args' => 'preset, url, message (presets), body (custom JSON), secret (optional)',
        'actions_note'    => 'Each action\'s required args are marked with a <code>*</code> in the form. Missing required args cause the action to fail at execution time &mdash; the engine logs the failure to the execution row and stops the rest of the chain.',
        'actions_webhook_callout' => '<strong>The <code>send_webhook</code> action is a big topic of its own</strong> &mdash; chat presets, whole-object &ldquo;Full record&rdquo; payloads, HMAC signing, a Send-test button, background delivery with retries, and a health dashboard. It has a dedicated guide: <a href="help-webhooks.php"><strong>Webhooks guide &rarr;</strong></a>',

        // 5. Variables
        'variables_heading' => '5. Variables &mdash; <code>{{path.to.field}}</code>',
        'variables_intro'   => 'Any free-text action arg can pull values out of the event that fired it. You don\'t have to type the codes by hand: every variable-friendly field has an <strong>Edit with variables</strong> button that opens a roomy editor with a <strong>searchable list of variables down the side</strong> — click one and it drops in at your cursor. A live preview shows the finished text with each code swapped for what it stands for.',
        'variables_scoped'  => '<strong>You are only ever offered variables the trigger can actually fill in.</strong> This matters more than it sounds: a missing variable doesn\'t raise an error, it quietly resolves to an <em>empty string</em>. So a workflow on <code>knowledge.published</code> whose message says <code>{{ticket.subject}}</code> wouldn\'t fail — it would just post a blank where the subject should be, and you\'d never know. There is no ticket in a knowledge event, so the picker doesn\'t offer ticket variables at all, and if you type one anyway the field warns you it will come out blank.',
        'variables_names'   => '<strong>Use the name, not the id.</strong> <code>{{ticket.priority_id}}</code> renders as <code>4</code> &mdash; a number nobody reading your Slack channel can interpret. Every lookup field therefore has a readable twin: <code>{{ticket.priority_name}}</code> renders <strong>Critical</strong>. The picker lists the name as the plain choice (<em>Ticket &middot; Priority</em>) and marks the numeric one <em>(id)</em>, because the name is almost always the one you want in a message a person will read. The ids are still there when you need them &mdash; for a condition, or for a receiving system that expects them. You also get <code>{{old_priority_name}}</code> / <code>{{new_priority_name}}</code> on the change triggers, so "escalated from High to Critical" writes itself.',
        'variables_number'  => '<strong>Quote the reference, not the row id.</strong> <code>{{ticket.id}}</code> is the internal database id (<code>83</code>) &mdash; meaningless to a requester. <code>{{ticket.number}}</code> is the reference they actually quote back at you (<code>ETC-623-64409</code>), and it&rsquo;s what belongs in an email subject or a chat alert. Same for <code>{{problem.number}}</code>. <strong>One exception:</strong> a <em>Ticket ID</em> action argument must stay <code>{{ticket.id}}</code> &mdash; that field tells the engine <em>which record to act on</em>, and it looks it up by row id. Use the number in the words a person reads; leave the id in the plumbing.',
        'variables_full'    => 'Where a trigger carries a whole record, you also get <code>{{&lt;object&gt;.full}}</code> &mdash; e.g. <code>{{ticket.full}}</code> &mdash; which embeds the entire record as JSON, in the exact shape the REST API returns it. And <code>{{event}}</code> always resolves to the name of the trigger that fired.',
        'variables_common'  => 'Common variables for ticket triggers:',
        'variables_li1' => '<code>{{ticket.id}}</code>, <code>{{ticket.subject}}</code>, <code>{{ticket.priority_id}}</code>, <code>{{ticket.status_id}}</code>',
        'variables_li2' => '<code>{{ticket.department_id}}</code>, <code>{{ticket.type_id}}</code>, <code>{{ticket.assigned_analyst_id}}</code>',
        'variables_li3' => '<code>{{ticket.owner_id}}</code>, <code>{{ticket.created_by}}</code>, <code>{{ticket.requester_email}}</code>',
        'variables_li4' => 'For <code>ticket.status_changed</code> / <code>ticket.priority_changed</code>: also <code>{{old_status_id}}</code>, <code>{{new_status_id}}</code>, <code>{{old_priority_id}}</code>, <code>{{new_priority_id}}</code>',
        'variables_li5' => 'For <code>ticket.assigned</code>: also <code>{{analyst_id}}</code>, <code>{{team_id}}</code>',
        'variables_tip' => '<strong>Best practice:</strong> in action <code>ticket_id</code> fields, leave the default <code>{{ticket.id}}</code> rather than typing a specific id &mdash; that way the workflow operates on whichever ticket triggered it, not a fixed one.',

        // 6. AI co-author
        'ai_heading' => '6. AI co-author',
        'ai_intro'   => 'Click <strong>AI co-author</strong> on the toolbar and describe the workflow in plain English. The AI returns a structured proposal &mdash; trigger, conditions, actions &mdash; that you can <em>Apply</em> to the canvas or <em>Discard</em>.',
        'ai_examples' => 'Examples that work well:',
        'ai_ex1' => '<em>"When a Critical-priority ticket is created, add a note saying \'P1 &mdash; please respond within 15 minutes\'."</em>',
        'ai_ex2' => '<em>"When a ticket from <code>finance@acme.com</code> is created, assign it to me and set priority to High."</em>',
        'ai_ex3' => '<em>"When a ticket is closed, send an email to the requester thanking them and asking for feedback."</em>',
        'ai_catalogue' => 'The AI knows the engine\'s full catalogue &mdash; every trigger, condition operator, action, and the available lookup-table values &mdash; so it can only propose things that will actually work. If you iterate on an existing workflow (<em>"now only match Finance"</em> / <em>"add an action to log the ticket id"</em>) it edits what\'s on the canvas rather than starting over.',
        'ai_config'    => 'Configure the provider (Anthropic / OpenAI), model and API key under <strong>Workflow &rarr; Settings &rarr; AI</strong>. Keys are stored per module so billing and access can be granular.',

        // 7. Saving and testing
        'templates_heading' => 'Start from a template',
        'templates_intro'   => 'The <strong>New from template</strong> button on the Workflows list opens a gallery of ready-made recipes for the jobs most service desks automate first &mdash; auto-acknowledging a new ticket, alerting the team on a P1, fanning a new-starter form out into IT, HR and Facilities tickets, raising an implementation task when a change is approved. Four of them are <strong>time-based</strong>: escalating a ticket <em>before</em> its SLA breaches, alerting when one has breached, and reminding you that a contract or an asset warranty is about to expire. Pick one and it is copied into your workflows as a normal, fully editable workflow. There is nothing special about it afterwards: it is yours to rename, rewire or delete. The gallery filters by <strong>category</strong> and by <strong>search</strong> &mdash; which reaches the description, the trigger and the actions too, so typing <em>expiry</em>, <em>slack</em> or <em>wipe</em> finds the recipe even when the name does not contain the word.',
        'templates_lookups' => 'Every install names its own statuses and priorities, so a template cannot simply hardcode them. Instead it says <em>"whatever this install calls Closed"</em> and we look the real value up as it is copied. Anything we cannot match &mdash; a priority you do not have, or a value only you know, like a Slack webhook URL &mdash; is left blank and listed in a banner at the top of the editor, so you can see exactly what still needs you.',
        'templates_scheduled' => 'The four time-based recipes need a <strong>scheduled task</strong> to be running, because nothing they watch for is an event &mdash; no-one saves anything when an SLA quietly runs out of time, so a job has to go looking. If the crons are not scheduled these workflows sit there, active, and never fire. The two expiry reminders also fire at <strong>90, 30, 7 and 1 days</strong> out, and each recipe filters to the 30-day one &mdash; change that condition, or drop it, to be told at every window. See <a href="#triggers">Triggers</a>.',
        'templates_callout' => 'A cloned template always arrives <strong>switched off</strong>. Nothing it contains can fire until you have read it, filled in the gaps and ticked <em>Active</em> &mdash; and you can <strong>Dry run</strong> it first to see precisely what it would do.',

        'testing_heading' => '7. Saving and testing',
        'testing_save'    => 'The status pip in the toolbar shows <em>Unsaved</em> / <em>Saving&hellip;</em> / <em>Saved</em>. Click <strong>Save</strong> to persist. You can save in-progress drafts with zero actions &mdash; you\'ll get a soft "this workflow has no actions yet" warning rather than being blocked.',
        'testing_dry_heading' => 'Dry run &mdash; see what it would do, without doing it',
        'testing_dry_body'    => '<strong>Dry run</strong> evaluates the conditions for real, then <em>describes</em> each action instead of carrying it out. The run appears in <em>Recent runs</em> marked <em>Dry run</em>, listing every action it would have taken with the <code>{{variables}}</code> already filled in &mdash; so you read the actual email subject, the actual chat message, the actual note.',
        'testing_dry_safe'    => 'Nothing is written, emailed, queued or sent: no ticket changes, no notes, no webhook deliveries, and the workflow\'s run counter is untouched. That makes it safe to point at a live workflow &mdash; and it is the sensible first thing to do with a freshly cloned template. (Any signing secret is redacted in the output rather than echoed back.)',
        'testing_log_heading' => 'The execution log &mdash; what actually happened',
        'testing_log_body'    => 'Every run of every workflow lands in the <strong>Execution log</strong> (in the Workflows header). Filter by workflow, status, trigger, date or free text, and open any run to see <strong>step by step</strong> what the engine did &mdash; which condition matched and which didn\'t (with the <em>actual value</em> it compared against), what each action returned or how it failed, and the <strong>trigger payload snapshot</strong>: exactly what the conditions were tested on and what the <code>{{variables}}</code> resolved from, captured at run time.',
        'testing_log_why'     => 'This matters more than it sounds. The engine <strong>deliberately swallows its own errors</strong> &mdash; a broken workflow must never break the ticket save that triggered it. That is the right call, and it means a failing workflow is otherwise <em>completely silent</em>. Nothing tells you. The execution log, and the <strong>Workflows card on Watchtower</strong> (which surfaces failures, loop-protection aborts and webhooks that gave up retrying, in the last 24 hours), are what break that silence.',
        'testing_fire_heading' => 'Test fire &mdash; actually run it',
        'testing_fire'    => '<strong>Test fire</strong> runs the workflow for real with a synthetic payload generated from its own conditions, so the action path actually executes &mdash; it will genuinely send that email. The result lands in <em>Recent runs</em> with status (<em>success / failed / skipped</em>) and full step log.',
        'testing_real'    => 'To test against real data, just do the thing that triggers it &mdash; assign a ticket, change its priority, etc. The dispatch from the host module (Tickets) is live; the execution log shows every fire.',

        // 8. The trigger catalogue
        'triggers_heading' => '8. The trigger catalogue',
        'triggers_intro'   => 'The catalogue now spans <strong>every module &mdash; 138 triggers and counting</strong>, all wired and live. They come in two kinds:',
        'triggers_time_heading' => 'Time-based triggers &mdash; and the cron they need',
        'triggers_time_body'    => 'Most triggers fire from a <strong>write path</strong>: someone saved a ticket, so there is a moment to fire from. Four don\'t. <code>sla.warning</code>, <code>sla.breached</code>, <code>contract.expiring</code> and <code>asset.warranty_expiring</code> aren\'t events at all &mdash; <em>nothing happened, time passed</em> &mdash; so a scheduled job has to go looking for them.',
        'triggers_time_cron'    => '<strong>If those crons aren\'t scheduled, these triggers never fire.</strong> The workflow will sit there, active, doing nothing. SLA events come from the SLA breach-check cron (every 5 minutes); the expiry events from <code>cron/workflow_scheduled.php</code> (hourly is plenty). Setup: <code>docs/workflow-scheduled-cron-setup.md</code>, or <a href="https://github.com/mymakecoins/domus-desk/wiki/Time-Based-Triggers" target="_blank" rel="noopener noreferrer">Time-Based Triggers on the wiki</a>, which also explains why a still-true condition (a breached SLA stays breached) doesn\'t re-fire every few minutes.',
        'triggers_time_once'    => 'A time-based condition <strong>stays true</strong> &mdash; a breached SLA is still breached five minutes later &mdash; so Domus Desk keeps a ledger and each situation fires <strong>exactly once</strong>. It re-arms when the thing underneath it changes: raise a ticket\'s priority and its SLA target shrinks, so the new, tighter deadline is allowed to escalate again. Renew a contract and next year\'s reminders still fire. Expiries fire at <strong>90, 30, 7 and 1 days</strong> out, each carrying <code>window_days</code> &mdash; so "only remind me at 30 days" is just a condition.',
        'triggers_family_domain' => '<strong>Rich domain events</strong> &mdash; meaningful lifecycle moments with a full typed payload: <code>ticket.created</code>, <code>ticket.status_changed</code>, <code>change.approved</code>, <code>problem.status_changed</code>, <code>task.completed</code>, <code>service_status.incident_resolved</code>, <code>software.application_discovered</code>, and many more.',
        'triggers_family_crud'   => '<strong>Create / update / delete events</strong> &mdash; every reusable record and settings lookup emits <code>&lt;entity&gt;.created</code> / <code>.updated</code> / <code>.deleted</code>: tickets, assets, changes, problems, tasks, CMDB, contracts &amp; suppliers, calendar, software licences, network diagrams, and all their settings lists (statuses, priorities, types, tags&hellip;).',
        'triggers_picker'  => 'Because the list is dozens deep, the editor\'s <strong>trigger picker is searchable</strong> &mdash; start typing (<code>resolved</code>, <code>contract</code>, <code>delete</code>&hellip;) to filter it. Each event fires from a <strong>single shared write path</strong>, so it behaves identically whether the change came from the browser, the REST API, or another workflow &mdash; it can\'t drift.',

        // 9. Failures
        'failures_heading' => '9. Engine failures are isolated',
        'failures_callout' => '<strong>A buggy workflow cannot break the host module\'s request.</strong> Every dispatch is wrapped in a try/catch and every action\'s failure is logged to the execution row without aborting the chain. If a workflow stops doing what you expect, check the workflow\'s <em>Recent runs</em> panel &mdash; failures show up there with the underlying error, not as a 500 on the page that triggered them.',

        // 10. What's still ahead
        'ahead_heading' => '10. What\'s still ahead',
        'ahead_li1' => '<strong>Tier 3 actions</strong> &mdash; Microsoft Graph: <code>graph_add_to_group</code>, <code>graph_assign_license</code>, <code>graph_disable_user</code>. The new-starter / leaver automations that justify the module\'s existence vs paid ITSM suites. Reuses the existing tenant OAuth scaffolding.',
        'ahead_li4' => '<strong>Dry-run mode</strong> &mdash; run a workflow against a real event but <em>log</em> the actions rather than execute them. So you can see what would have happened before flipping it live.',
        'ahead_li5' => '<strong>Starter recipes</strong> &mdash; clonable templates for the common patterns: new-starter onboarding, P1 incident response, SLA-breach escalation, license-renewal reminder.',
        'ahead_li6' => '<strong>Watchtower integration</strong> &mdash; failed runs surface as attention cards.',
        'ahead_li7' => '<strong>Workflow versioning</strong> &mdash; draft / publish / rollback.',
        'ahead_li8' => '<strong>Streaming AI co-author</strong> &mdash; claude.ai-style live output as the canvas builds.',
    ],
];
